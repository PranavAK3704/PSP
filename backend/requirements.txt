fastapi==0.115.0
uvicorn[standard]==0.30.6
requests==2.32.3
pyyaml==6.0.2
python-dotenv==1.0.1
sse-starlette==2.1.3
python-multipart==0.0.18  # multipart form parsing for the doc-upload endpoints (>=0.0.18: CVE-2024-53981)
pypdf==4.3.1              # PDF text extraction for uploaded docs
openpyxl==3.1.5          # Excel (.xlsx) extraction for uploaded SOP sheets
python-docx==1.1.2       # Word (.docx) extraction for uploaded SOP docs
# anthropic — required when provider: claude (models.yaml). Floor is deliberate:
# 0.39.x passes a `proxies` kwarg that httpx>=0.28 removed, so it crashes on client init.
anthropic>=0.122.0
certifi==2024.8.30        # TLS CA bundle for the Turso HTTPS data source
